
let { expect } = require("chai");

describe("A test suite", function() {
   beforeEach(function() { });
   afterEach(function() { });
   it('is good', function() { expect(lib1.a).to.be.valid; });
});
